# Rollback package

This package restores the site files to the exact site source provided by the user in `testpack-dashboard-site(2).zip`, excluding only the `.git` folder to make it safe to upload/deploy.

No SQL, D1 reset, Selenium, or workflow changes are included.
